# auto-generated by grc.converter

id: ccsds_encapsulate_net
label: encapsulate_net
category: '[CCSDS]'

parameters:
-   id: mtu
    label: MTU Size
    dtype: int
-   id: pad_byte
    label: Pad Byte
    dtype: raw
-   id: aggregate
    label: Aggregate Packets
    dtype: bool
-   id: timeout
    label: Timeout (ms)
    dtype: int

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import gnuradio.ccsds as ccsds
    make: ccsds.encapsulate_net(${mtu}, ${pad_byte}, ${aggregate}, ${timeout})

file_format: 1
